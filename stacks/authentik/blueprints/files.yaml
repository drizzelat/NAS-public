# Generated to match `ak export_blueprint`; see docs/services/authentik.md.
# client_secret is deliberately absent - it stays DB state, never git.
version: 1
metadata:
  name: NAS - files OIDC provider
entries:
- model: authentik_providers_oauth2.oauth2provider
  identifiers:
    name: files
  attrs:
    access_code_validity: minutes=1
    access_token_validity: minutes=5
    authentication_flow: null
    authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
    client_id: HhOnQaESQBV7Rnvr9BxtGWYzcE9WGh2ufiVAGyA1
    client_type: confidential
    encryption_key: null
    grant_types:
    - authorization_code
    - refresh_token
    include_claims_in_id_token: true
    invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
    issuer_mode: per_provider
    jwt_federation_providers: []
    jwt_federation_sources: []
    logout_method: frontchannel
    logout_uri: ''
    name: files
    property_mappings:
    - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
    - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
    - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
    redirect_uris:
    - matching_mode: strict
      redirect_uri_type: authorization
      url: https://files.example.com/api/auth/oidc/callback
    refresh_token_threshold: hours=1
    refresh_token_validity: days=30
    signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
    sub_mode: hashed_user_id
  id: provider
- model: authentik_core.application
  identifiers:
    slug: files
  attrs:
    backchannel_providers: []
    group: ''
    meta_description: ''
    meta_hide: false
    meta_icon: ''
    meta_launch_url: ''
    meta_publisher: ''
    name: files
    open_in_new_tab: false
    policy_engine_mode: any
    provider: !KeyOf provider
    slug: files
